{
  "_comment": "Pass 1 domain rules. First match wins, so list more specific rules before more general ones (e.g. calendar.google.com before google.com). Pattern semantics: 'host.com' matches the bare host AND any subdomain. '*.host.com' matches subdomains only. Edit and click the button — no restart needed.",
  "rules": []
}
